function Sort(props) {
    return (
        <div>
            <i
                className="fa-solid fa-arrow-up-a-z"
                onClick={() => alert("TODO: open sort by label")}
            ></i>
            <p className="help-text">Sort</p>
        </div>
    );
}

export default Sort;
